# Development Dockerfile for backend
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY tsconfig.json ./
COPY src ./src

ENV NODE_ENV=development
EXPOSE 5000

CMD ["npm", "run", "dev"]
