apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: prod
  labels:
    app: backend
    env: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
      env: prod
  template:
    metadata:
      labels:
        app: backend
        env: prod
    spec:
      containers:
        - name: backend
          image: dasujandb/support-chat-backend:prod-latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          env:
            - name: PORT
              value: "5000"
            - name: NODE_ENV
              value: "production"
            - name: CLIENT_URL
              value: "http://13.203.161.35:30022"
            - name: CHAT_PRIMARY_COLOR
              value: "#2563EB"
            - name: CHAT_SECONDARY_COLOR
              value: "#EFF6FF"
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: prod
spec:
  type: NodePort
  selector:
    app: backend
    env: prod
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30021
